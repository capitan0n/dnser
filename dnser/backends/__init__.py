"""DNS backend implementations.

Import from the concrete modules (`dnser.backends.base`, `.detect`,
`.resolved`, `.networkmanager`) rather than from this package — there are
deliberately no re-exports here to keep a single obvious import path.
"""
